* router should gain `base_url = ""` and `base_path = getwd()` parameters
  * router should gain render_brew, redirect and error, static_file methods

* render 

* absolute paths should be preserved (don't prefer)
* more R like interface
* vignette explaining how it works
* complete other HTTP verbs
* layout templates

* splat of "/*" should not match the route of "/"